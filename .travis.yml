language: python
cache: pip
python:
  - "3.6"
before_install:
  - export DJANGO_SETTINGS_MODULE="meetpy.settings.local"
install:
  - pip install -r requirements/base.txt
before_script:
  - cd meetpy
  - ./manage.py migrate
script:
  - ./manage.py test
