language: python
cache: pip
python:
  - "3.6"
before_install:
  - cp .env.ci .env
install:
  - pip install -r requirements/base.txt
before_script:
  - cd meetpy
  - ./manage.py migrate
script:
  - pytest
